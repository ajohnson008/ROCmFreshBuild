# container.def - Rootless Podman configuration for TheRockBuilder
# Strict NVIDIA isolation + offline build environment

FROM docker.io/library/ubuntu:24.04

# ============================================================================
# BASE SYSTEM: Minimal build environment
# ============================================================================
RUN apt-get update && apt-get install -y --no-install-recommends \
    build-essential \
